services:
  api:
    build: ./backend
    restart: unless-stopped
    environment:
      DATABASE_URL: sqlite:////data/estoque.db
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./backups:/backups

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - api

  tests:
    profiles: ["test"]
    build:
      context: ./backend
      dockerfile: Dockerfile.test
    environment:
      DATABASE_URL: sqlite:////tmp/estoque_test.db
    command: ["pytest", "-q"]
